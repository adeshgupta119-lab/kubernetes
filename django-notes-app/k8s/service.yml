apiVersion: v1
kind: Service
metadata:
  name: notes-app-service
  namespace: nginx

spec:
  type: ClusterIP

  selector:
    app: notes-app

  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
    
